# PostgreSQL. Versions 7.4 and 8.x are supported.
#
# Install the ruby-postgres driver:
#   gem install ruby-postgres
# On Mac OS X:
#   gem install ruby-postgres -- --include=/usr/local/pgsql
# On Windows:
#   gem install ruby-postgres
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
development:
  adapter: postgresql
  encoding: unicode
  database: geomodeler_development
  username: geomodeler
  password: geomodeler

test:
  adapter: postgresql
  encoding: unicode
  database: geomodeler_test
  username: geomodeler
  password: geomodeler

production:
  adapter: postgresql
  encoding: unicode
  database: geomodeler_production
  username: geomodeler
  password: geomodeler
